#This is an example script for using tuples.

#Different from lists, tuples are initialized with () rather than []:
myTuple = ('hello', 42, 0.5)

#While lists are mutable, tuples are immutable. Values cannot be appended, modified, or removed.
#Lists can be converted to tuples, and vice versa.
